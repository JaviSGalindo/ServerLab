import { getServerURL } from "./task1.js";
import { listUsers } from "./task2.js";

async function delUser(id) {
    try {
        
    } catch (error) {
        console.error('Error en la función delUser:', error);
    }
}

// Llamar a la función para eliminar el usuario con ID 6
delUser(6);

export { delUser };

